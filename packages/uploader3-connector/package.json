{
  "name": "@lxdao/uploader3-connector",
  "version": "0.0.0",
  "main": "./lib/index.js",
  "module": "./esm/index.js",
  "files": [
    "lib",
    "esm"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf lib esm",
    "build:lib": "tsc -m commonjs --outDir lib",
    "build:esm": "tsc -m esnext --outDir esm",
    "build": "npm run clean && npm run build:lib && npm run build:esm",
    "lint": "TIMING=1 eslint \"**/*.ts*\"",
    "test": "echo 0; exit 0"
  },
  "devDependencies": {
    "typescript": "^4",
    "@types/node": "^17.0.45",
    "tsconfig": "workspace:*"
  },
  "dependencies": {
    "nft.storage": "^7.0.0"
  }
}
